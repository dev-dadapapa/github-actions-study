name: branch-filter
on:
    push:
        tags:
        - 'v[0-9]+.[0-9]+.[0-9]+'  # v1.0.0 or v2.2.2  (예외: v1.0 or 1.0.0)

jobs:
    branch-filter:
        runs-on: ubuntu-latest
        steps:
        - name: echo
          run: echo "is this dev?"