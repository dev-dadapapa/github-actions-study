name: secrets
on: [push]

jobs:
  get-secret:
    runs-on: ubuntu-latest
    if:  ${{ secrets.key}} == "repo-sec"
    steps:
      - name: get secrets
        run: |
        echo "checked secret : ${{ secrets.key}} "